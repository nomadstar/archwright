# NNNN — Title

**Status:** draft | accepted | rejected | superseded by NNNN

## Summary

One paragraph: what changes, in plain terms.

## Motivation

What's wrong or missing today that this fixes. Link to an issue, an ADR,
or a concrete workspace that hit the limitation.

## What breaks

Be exact. Which spec document(s) does this change? Which currently-valid
workspaces stop validating, or start behaving differently, once this
ships?

## Migration

What does a workspace author have to do to their existing files to keep
working after this change? If the answer is "nothing, this is additive,"
say so explicitly and reconsider whether this needed an RFC at all (see
`docs/rfcs/README.md`).

## Alternatives considered

What else was weighed, and why it lost to this proposal.

## Spec version impact

Does `ARCHWRIGHT_SPEC_VERSION` need to bump? (See
`docs/decisions/0006-spec-versioned-separately-from-engine.md`.)
